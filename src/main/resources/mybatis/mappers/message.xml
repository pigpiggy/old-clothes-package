<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
						
<mapper namespace="com.kosta.clothes.dao.MessageDAO">
	<insert id="insertMessage" parameterType="MessageVO">
	<selectKey keyProperty="mno" resultType="Integer" order="BEFORE">
		<![CDATA[
			select message_seq.nextval from dual
		]]>
	</selectKey>		
		<![CDATA[
			insert into message (mno,mtitle,mcontent,sendUserno,recvUserno,readChk,regdate)
			values (#{mno},#{mtitle},#{mcontent},#{sendUserno},#{recvUserno},0,sysdate)
		]]>
	</insert>
	
</mapper>	