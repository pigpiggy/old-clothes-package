<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
						"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
						
<mapper namespace="com.kosta.clothes.dao.MypageDAO">
	<select id="getSellList" parameterType="Integer" resultType="Sell">
		<![CDATA[
			SELECT * FROM INDIVIDUAL WHERE userno=#{userno} ORDER BY ino DESC
		]]>
	</select>
	

	<select id="getSharingList" parameterType="Integer" resultType="Sharing">
		<![CDATA[
			SELECT * FROM sharing WHERE userno=#{userno} ORDER BY sno DESC
		]]>
	</select>
	
	<select id="getSellapplylist" parameterType="Integer" resultType="String">
		<![CDATA[
			SELECT * FROM wapply WHERE ino = #{ino}
		]]>
	</select>

	<insert id="uintroduce" parameterType="map">
		<![CDATA[
			update users set
			introduce = #{introduce} where userno = #{userno}
		]]>
		</insert>
		
		<select id="uintromyself" parameterType="Integer" resultType="String">
		<![CDATA[
			SELECT INTRODUCE FROM USERS WHERE userno=#{userno}

		]]>
	</select>
	
	<select id="getMypage" parameterType="Integer" resultType="users">
		<![CDATA[
			SELECT * FROM USERS WHERE userno=#{userno}

		]]>
	</select>
</mapper>	